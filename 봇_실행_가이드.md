# 🤖 봇 모드 실행 가이드

## 문제: 멘션했는데 응답이 없어요!

**원인:** 봇 모드(Socket Mode)가 실행되고 있지 않습니다.

GitHub Actions는 **Webhook 모드**만 사용합니다:
- ✅ 자동 전송: 매일 정해진 시간에 자동으로 메뉴 전송
- ❌ 멘션 기능: 작동하지 않음 (봇 모드 필요)

멘션 기능을 사용하려면 **봇 모드를 별도로 실행**해야 합니다.

---

## 🚀 봇 모드 실행 방법

### 방법 1: 로컬 컴퓨터에서 실행 (개인용)

1. **`.env` 파일 설정**
   ```env
   SLACK_BOT_TOKEN=xoxb-your-bot-token-here
   SLACK_APP_TOKEN=xapp-your-app-token-here
   SCHOOL_MENU_WEBSITE_URL=https://mportal2.cau.ac.kr/main.do
   SELENIUM_HEADLESS=false  # 테스트용
   ```

2. **봇 실행**
   ```bash
   python app.py
   ```

3. **결과 확인**
   - "🚀 Slack 학교 급식 메뉴 봇이 시작되었습니다!" 메시지가 나타나면 성공
   - 이제 Slack에서 `@봇이름 메뉴` 입력하면 응답합니다!

---

### 방법 2: 서버에서 실행 (24시간 운영)

#### 옵션 A: 무료 서버 사용 (Heroku, Railway 등)

1. **Heroku 사용 예시:**
   ```bash
   # Heroku CLI 설치 후
   heroku create your-bot-name
   heroku config:set SLACK_BOT_TOKEN=xoxb-...
   heroku config:set SLACK_APP_TOKEN=xapp-...
   heroku config:set SCHOOL_MENU_WEBSITE_URL=https://...
   git push heroku main
   ```

#### 옵션 B: 개인 서버 사용

1. **서버에 코드 업로드**
2. **`.env` 파일 설정**
3. **백그라운드 실행:**
   ```bash
   nohup python app.py > bot.log 2>&1 &
   ```

#### 옵션 C: GitHub Actions로 봇 모드 실행 (고급)

`.github/workflows/run_bot_socket.yml` 파일 생성:

```yaml
name: 봇 모드 실행

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run bot
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_APP_TOKEN: ${{ secrets.SLACK_APP_TOKEN }}
          SCHOOL_MENU_WEBSITE_URL: ${{ secrets.SCHOOL_MENU_WEBSITE_URL }}
        run: python app.py
```

⚠️ **주의:** GitHub Actions는 워크플로우가 끝나면 종료되므로, 봇 모드를 계속 실행하려면 다른 서비스가 필요합니다.

---

## ✅ 체크리스트

봇이 응답하지 않을 때 확인할 것:

- [ ] `app.py`가 실행되고 있는가?
- [ ] `.env` 파일에 `SLACK_BOT_TOKEN`과 `SLACK_APP_TOKEN`이 설정되어 있는가?
- [ ] Slack 앱에서 Socket Mode가 활성화되어 있는가?
- [ ] 봇이 채널에 초대되어 있는가?
- [ ] Slack 앱 권한에 `app_mentions:read`가 추가되어 있는가?

---

## 💡 추천 방법

**개인 사용:** 로컬 컴퓨터에서 `python app.py` 실행 (컴퓨터가 켜져 있을 때만 작동)

**24시간 운영:** Heroku, Railway, Render 같은 무료 서비스 사용

**자동 전송만 필요:** GitHub Actions 사용 (현재 설정대로 사용)

